#include"animation.h"

void animation::addFrame(PNG const & img) {
	frames.push_back(img);
}

int animation::size() {
	return frames.size();
}

const PNG & animation::frame(int frameNum) {
	return frames[frameNum];
}
